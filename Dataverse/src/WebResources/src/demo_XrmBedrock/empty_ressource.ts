/* This function is a scaffold to use when developing web resources, so we can avoid having to deploy a new scaffold
everytime we want to create a new web resource. Instead, this empty scaffold can be applied and used with fiddler */
namespace test {
    import EntityList = Xrm.EntityList;
    import EntityReference = Xrm.EntityReference;
 
    export function onLoad(context: Xrm.ExecutionContext<any, any>) {
 
    }
 
    export function onSave(context: Xrm.ExecutionContext<any, any>) {
 
    }
}